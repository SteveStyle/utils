/// This module contains utility functions that are used in the project.
/// To use this crate, add `utils = { path = "utils" }` to the dependencies in Cargo.toml.
/// Alternatively, use the git repository by adding `utils = { git = ..."
pub mod timer;
